builds:
  - binary: sern
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
    main: ./cmd
signs:
  - artifacts: checksum
    args: ["--batch", "-u", "{{ .Env.GPG_FINGERPRINT }}", "--output", "${signature}", "--detach-sign", "${artifact}"]
